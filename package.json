{
  "name": "donte",
  "private": true,
  "description": "Donte crowdfunding — single deploy: Express API + Vite client",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/index.js",
    "build:deploy": "npm install --prefix server && npm install --prefix client && npm run build --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
